import { makeStyles } from "tss-react/mui"

export const useStyles = makeStyles()(() => ({
  box: {
    padding: "30px 0",
    textAlign: "center",
    fontSize: "15px",
    backgroundColor: "white",
    width: "50%"
  }
}))
